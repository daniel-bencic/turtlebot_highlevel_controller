namespace turtlebot_highlevel_controller {
        struct PolarPoint {
                PolarPoint();
                PolarPoint(float dist, float angle);

                float dist, angle;
        };
}
